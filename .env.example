# A generated secret for Payload
PAYLOAD_SECRET=

# Shared secret for the manual cache-purge route (POST /api/revalidate).
# Generate with `openssl rand -hex 32`. Used after `pnpm sync:prod`.
REVALIDATE_SECRET=
